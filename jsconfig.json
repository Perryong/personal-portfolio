
{
    "compilerOptions": {
      "baseUrl": ".",         // let imports start from project root
      "paths": {
        "@/*": ["src/*"]      // "@/…" -> "src/…"
      }
    },
    "exclude": ["dist", "node_modules"]
  }
  